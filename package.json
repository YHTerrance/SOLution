{
  "scripts": {
    "test": "anchor test --provider.cluster localnet",
    "build": "anchor build --provider.cluster localnet",
    "build:devnet": "anchor build --provider.cluster devnet",
    "build:mainnet": "anchor build --provider.cluster mainnet",
    "deploy": "anchor deploy --provider.cluster localnet",
    "deploy:devnet": "anchor deploy --provider.cluster devnet",
    "deploy:mainnet": "anchor deploy --provider.cluster mainnet",
    "lint": "vue-cli-service lint",
    "pretty": "prettier --write \"./**/*.{js,jsx,json}\""
  },
  "dependencies": {
    "@project-serum/anchor": "^0.22.1"
  },
  "devDependencies": {
    "@types/chai": "^4.3.0",
    "@types/mocha": "^9.0.0",
    "chai": "^4.3.4",
    "mocha": "^9.0.3",
    "ts-mocha": "^10.0.0",
    "typescript": "^4.3.5"
  }
}
